# AI 使用记录

---

## （2026-09-09）——KFC 角色枚举、精简评估与补全

**聊了什么：** 我让 AI 列出 KFC 门店中所有角色与职能（如店长、店员、会员）。AI 按"门店运营层 / 管理层 / 后台支持层 / 外部角色"四层枚举了 21 个角色（服务员——细分外场/内场/咖啡师、训练员、收银员、配餐员/总配员、厨师、厨师长、楼面经理、值班经理、副经理、资深副经理、储备经理、餐厅经理/店长、区经理、人力/财务/采购专员、散客、WOW 会员、付费会员、品牌挚友、第三方骑手）。我又追问：若只做单店的基本经营闭环，选训练员、收银员、配餐员、厨师、值班经理、餐厅经理、HR、散客、WOW 会员、付费会员这 10 个角色是否足够。

**AI 做了什么：** 逐项代入业务流程"沙盘推演"，确认这组角色能跑通 下单→制作→配餐→收银→值班管理→会员留存 的核心闭环，但指出两个缺口——缺基础"服务员"（外场基础勤务若让训练员/收银员兼任，权限设计上冗余）；缺"第三方骑手"（若做外送，订单履约的已取餐/配送中状态会缺关联数据）。

**我的处理：** 采纳建议，**新增"服务员"与"第三方骑手"两个角色**，角色体系补全，并按照选择的角色职责进行整理，再让AI进行补充和扩展。

## （2026-09-09）——业务全貌梳理 + 两份设计清单

**聊了什么：** 把项目背景讲给 AI：以现实 KFC 门店为原型，维护一个数据库，平台 SQL Server。业务主线是后台准备 → 顾客点餐计价 → 下单支付 → 后厨制作 → 交付配送 → 终态积分/库存处理 → 补货闭环。同时逐个讲了关键机制：店长维护商品+BOM、促销配置（如疯狂星期四）、下单时实时匹配促销并写成交价快照、支付成功后两段式积分（先记待生效）、下单即锁定原料防超卖、制作完成才实扣库存、终态触发积分到账与升级、扣库存后低于安全库存线自动生成补货建议单。并让AI优化了两份设计清单和提出建议。

**AI 做了什么：** 把这套口述整理成结构化的业务流程与机制点描述；随后帮用户一起把两份设计清单优化成型——
- 角色与职能清单：分"后台 / 门店作业 / 顾客 / 系统自动行为"四类，逐角色列职责、触碰数据与权限边界建议，其中把系统自动行为单独拎出来作为权限主体。
- 数据边界清单：按"进事实与快照、不进实时态与过程态"的原则，列出必须入库、不进库和灰色地带三部分及理由。

**我的处理：** 逐条对照真实门店经营核对内容；散客是否按手机号建档等灰色地带由我确定；清单确认后作为后续设计与建库的依据。

## （2026-09-09）——清单落盘 + 业务流程一致性审查 + 术语收尾

**聊了什么 / AI 做了什么：**
1. 把两份清单固化为仓库里的 markdown 文档（docs/），并整理 README 索引。
2. 我提交了《业务流程》文档，请 AI 与我之前提交的已有设计比对找冲突。AI 逐段核对后报告：主流程状态机（待支付→已支付→制作中→待取餐/待配送→配送中→已取餐/已完成）和成交价快照、两段式积分、锁定转实扣、安全库存线补货等机制都与设计一致；**唯一的实质冲突是"防超卖锁库的时点"**——角色清单/蓝图写的是"下单即锁"，而《业务流程》把锁库写在支付环节。此外指出流程里缺了"取消 / 退款 / 锁释放"整段。
3. 我采用**下单即锁**。AI 据此改《业务流程》：把锁库动作移到"下单确认生成订单"那一刻（锁定量↑、现有量不变、可售量↓），支付环节只保留积分预计算；新增第 8 步"异常路径"（未支付超时/取消 → 释放锁、订单置已取消；已支付在实扣前取消/退款 → 释放锁；退款同时冲销待生效积分流水）；补上外送"送达确认时记录送达时间"。
4. 术语收尾：统一全项目库存说法为**账面量（现有量）/ 锁定量 / 可售量（=现有量−锁定量）**，清掉含糊的"可用库存"；修了清单文件改名导致的 README 失效链接；修正一处从积分流水行复制错到配送记录行的理由文字。

**我的处理：** 锁定时点这类业务决策由我决定；AI 对文档的每处修改我都确认过，部分措辞由我自行再改。

---

## （2026-09-18）——第一阶段建库启动：C 域 P0 交付与批次规则修正

**聊了什么：** 我让 AI 按 AGENTS.md 指引通读整个仓库（README、docs 全部文档、第一阶段 plan、已有 SQL 与 git 历史），确认当前阶段、我的职责边界与三人进度；我定下本轮工作优先级——先做"我不依赖别人、别人依赖我"的任务，再做"不依赖别人"的任务，依赖别人的（P2）先不动；我决定本轮先交付 P0 为止。

**AI 做了什么：**
1. 盘点结论：设计文档阶段已完成、已进入第一阶段建库实施；A 已提交 `01_master_schema.sql`（10 张表、主外键、两个唯一/过滤唯一索引），B 尚无脚本；我名下 11 个交付物未开始，plan 的 76 个清单项均未勾选。
2. 按我定的优先级把 C 的 11 个交付物拆成 P0/P1/P2，并给出 P0 的具体实现方式；随后按 plan §2.1/§5 写出四个文件：`00_create_database.sql`（已存在即报错、建库、兼容级别 ≥130 校验）、`run_all.sql`（`:on error exit` + 18 条 `:r` 冻结顺序）、`03_inventory_security_schema.sql`（安全域 4 表 + 库存/补货/审计 6 表 + 命名键与过滤唯一索引 + `Delivery` 骑手外键）、`06_inventory_constraints_crud.sql` 第一段（15 个命名 CHECK，含 46 项 `permission_code` 授权矩阵白名单 + `sp_write_audit_log`）。
3. 编写静态自查脚本与临时实机验证工具（`Delivery` 临时桩、T1–T12 事务回滚断言、删库清理脚本）；静态自查全部通过，权限码 46 项无重复且与 plan 逐一对应。
4. 自查中发现并修正三处批次规则问题：普通 CREATE/ALTER/DML 批次要把 7 项 SET 写在同一批次开头；`CREATE DATABASE` 必须独占批次；`CREATE PROCEDURE` 必须是批次第一条语句（否则 Msg 111），所以过程批次要用"独立 SET 批次 + GO"再接 `CREATE PROCEDURE`。
5. 收尾时按我的要求删除了 `/tmp` 下全部临时验证文件，并把进度、P1/P2 计划与经验教训写入我的个人 AGENTS.md。

**我的处理：**
- 我确认"只动我名下文件，A/B 文件只审不改"的边界；四个 P0 文件的范围与内容由我审定后交付，别人依赖的建库/唯一入口/审计接口在这一档完成。
- 我决定先停在 P0：A、B 可开始各自独立开发（A 写 04/07a/09a/10a，B 写 02/05）；P1（库存调整与补货闭环过程、C 域视图、库存权限数据字典）与 P2（三个库存接口过程、订单追溯视图、08/09c/09d/10c 与全量联调）等后续再接。
- 实机验证按我的决定留到 Windows 环境执行；四个 P0 文件待我按 plan §6.3 的提交规范提交。

---

## （2026-09-20）——第一阶段 B 域验收脚本复核修正

**本次会话与同步：** 在已同步的远程 `origin`（`https://github.com/HZYoier/KFC-DB.git`）上下文中继续成员 B 的第一阶段订单履约工作，分支为 `codex/member-b-order`；本次变更前的提交上下文为 `2d70779`。

**AI 做了什么：** 按复核结论更新 `sql/10b_order_acceptance_tests.sql`：把存储过程 `EXEC` 中的内联 JSON、金额计算和交易号生成改为先赋值的本地变量，并把测试订单号收敛为不超过 40 字符的短前缀加 UUID 片段；保留既有积分快照、会员等级阈值、回滚和部分写入断言。更新 `docs/AI使用记录.md`，并写入本轮任务报告 `.superpowers/sdd/stage1-three-person-implementation-plan/task-5-report.md`。

**核对结果：** 使用 Microsoft SQL Server ScriptDom 对验收脚本做静态语法解析，结果为 0 个错误；同时完成 `EXEC` 参数、订单号长度和 `git diff --check` 的静态复核。该记录只描述本次实现和复核状态，不新增业务需求。

---

## （2026-09-21）——第一阶段 B 域本周交付静态验收

**聊了什么：** 继续完成成员 B 的第一阶段订单履约实现，并对当前分支做交付前复核。

**AI 做了什么：** 核对 B 域五个 SQL 脚本、订单履约数据字典和本记录；使用 SQL Server ScriptDom 解析建表、约束 CRUD、视图查询、种子和验收脚本；复核表、过程、视图、查询示例、种子订单数量，以及验收脚本参数和跨域调用边界。

**核对结果：** 五个 SQL 脚本静态解析均为 0 个错误；B 域交付契约为 5 张表、8 个过程、4 个视图、3 条具名查询和 3 笔集成种子订单；分支差异检查通过。运行时联调由后续完整 A/C 集成验收继续执行。

---

## （2026-09-21）——发布 A/C 可继续实现的跨域接口契约

**聊了什么：** 检查远端 A 当前实现，确认远端只有主数据建表和数据字典，尚未提供定价、积分、库存和审计的可执行跨域接口。

**AI 做了什么：** 将 B 的订单表结构、A→B 定价/积分接口、C→B 锁库/释放/实扣/审计接口，以及参数、返回列、状态值和嵌套事务要求整理为独立契约文档；增加只读接口签名检查脚本；把这部分从 `origin/main` 单独建立 `codex/interface-contracts` 分支并推送，供 A/C 继续实现。

**核对结果：** 契约分支只包含稳定的订单表、数据字典、接口契约和检查脚本；未创建会占用 A/C 对象名的空过程或函数，B 尚未确定的业务实现继续保留在本地成员 B 分支。

---

## （2026-09-26）——同步远端 A/C 实现到本地 B 分支

**聊了什么：** 同步远端 `origin/main` 的最新 A/C 代码，保留本地成员 B 的订单过程、视图、种子和验收脚本。

**AI 做了什么：** 将远端 A 的约束与 CRUD、主数据视图，以及 C 的库存过程、库存视图、角色权限和审计实现合并到 `codex/member-b-order`；解决 AI 使用记录的并行追加冲突，并检查合并后的脚本文件完整性。

**核对结果：** 合并提交为 `d416527`；14 个 SQL 文件均通过 ScriptDom 静态解析；本地 B 历史和最新 `origin/main` 均保留在合并分支中。当前仍需 C 补齐 `sp_receive_inventory` 后才能通过完整接口检查。

---

## （2026-09-26）——成员 B 与 A/C 本机接口联调

**聊了什么：** 基于同步后的 A/C 接口继续完成成员 B 可独立交付的订单履约工作，并用本机 SQL Server 做端到端联调。

**AI 做了什么：**
1. 在本机 `KFC_DB` 按依赖顺序部署 A、B、C 已提交的建表、约束/过程、视图脚本；使用临时测试数据和测试角色执行 B 的订单种子与验收脚本。
2. 修正 B 验收脚本中预期异常发生后的事务回滚/模拟用户恢复顺序，并将负向用例拆为相互独立的可回滚测试；补充套餐端到端联调断言，核对 B 订单父子项、A 套餐 BOM 视图及 C 锁库流水。
3. 修正只读跨域接口检查器对 `DATETIME2(0)` 元数据长度的期望值，并核对 A/C 现有 B 依赖接口的对象和参数元数据。

**核对结果：** `sql/09b_order_seed_data.sql` 成功创建 3 笔集成种子订单；`sql/10b_order_acceptance_tests.sql` 本机执行成功，全部负向校验、支付回滚、快照、套餐展开与锁库、积分升级、取餐、配送和退款用例均通过。验收测试事务回滚，未留下验收过程订单。

**我的处理：** 保留真实 A/C 过程作为联调对象，不创建占位接口；本轮改动留在本地集成分支，待确认后再决定是否发布。

---
